<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Merge_Ticket__c</fullName>
    <label>Merge Ticket</label>
    <description>Lookup to the originating ticket. Kept as Lookup (not Master-Detail) so the log is never cascade-deleted if the ticket is manually removed.</description>
    <deleteConstraint>SetNull</deleteConstraint>
    <referenceTo>Merge_Ticket__c</referenceTo>
    <relationshipLabel>Merge Logs</relationshipLabel>
    <relationshipName>Merge_Logs</relationshipName>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <type>Lookup</type>
</CustomField>
