<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Require_Closed_Reason_When_Ignored</fullName>
    <active>true</active>
    <description>Forces the reviewer to document why a ticket is being dismissed. Prevents silent data loss without justification.</description>
    <errorConditionFormula>AND(
            ISPICKVAL(Status__c, &apos;Ignored&apos;),
            ISPICKVAL(Closed_Reason__c, &apos;&apos;),
            ISBLANK(Notes__c)
        )</errorConditionFormula>
    <errorDisplayField>Closed_Reason__c</errorDisplayField>
    <errorMessage>Please select a Closed Reason or add a Note before dismissing this ticket.</errorMessage>
</ValidationRule>
